{
  "name": "web-tracks",
  "private": true,
  "type": "module",
  "description": "Web Tracks browser groovebox. No build step and no dependencies; package.json exists only to run the tests.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
